import React, { Fragment } from "react";

export default () => (
  <Fragment>
    <h2>micro-graphql-react live demo</h2>
    <h6>
      Search and page for books below. Check your network tab (json section) to
      see caching in action.
    </h6>
    <br />
  </Fragment>
);
